drush:
  aliases:
    remote: 'chew.dev'

command-hooks:
  frontend-reqs:
    dir: '${docroot}/themes/custom/chew'
    command: 'npm install; npm install gulp'
  frontend-assets:
    dir: '${docroot}/themes/custom/chew'
    command: 'gulp'

cm.core.dirs.sync.path: ${cm.core.path}/chew
