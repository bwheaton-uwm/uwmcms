{% extends "@uwmbase/layouts/layout-sections/layout-sections--content-no-grid.twig" %}
{#
/**
 * @file
 * Theme override for Res Clinic node pages.
 *
 * In default layout-sections.twig, the grid container and col classes are on
 * the section.content-main wrapper. On this page, grid containers, rows, and
 * columns are applied in the node template and with mixins.
 * @see components/clinic-page/node--res-clinic--full.html.twig
 * @see components/clinic-page/clinic-page.scss
 */
#}
