{% extends "@uwmbase/layouts/layout-sections/layout-sections--content-no-grid.twig" %}
{#
/**
 * @file
 * Theme override for Res Provider pages.
 *
 * Allows using a full-width layout for this content type,
 * so that the individual node template can also span 
 * full width.
 *
 * @see node--res-provider.html.twig
 *
 */
#}
