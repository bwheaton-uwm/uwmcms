{% extends "@uwmbase/layouts/layout-sections/layout-sections--no-cols.twig" %}
{#
/**
 * @file
 * Theme override for Medical Service node pages.
 *
 * In default layout-sections.twig, the grid container and col classes are on
 * the section.content-main wrapper. On this template, allow the templates of
 * components in the content (e.g. hero paragraph) to define grid rows/cols.
 *
 * @see paragraph--half-wide-hero.html.twig
 */
#}
